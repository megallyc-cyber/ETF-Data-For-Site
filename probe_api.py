"""Fix the bugs found auditing the pages.

Total return was measured from the market price and labelled "distributions
included"; the backtest chart read the price column as the adjusted close, so
both of its lines were wrong; three tickers sat in two sectors at once; medians
took the upper-middle value; and a fund with no published holdings drew zeros
and a fabricated "holds securities directly".
"""
import json, subprocess, ast
from pathlib import Path

PATCH = 'diff --git a/account/index.html b/account/index.html\nindex 2150643..3e97174 100644\n--- a/account/index.html\n+++ b/account/index.html\n@@ -898,7 +898,12 @@ licentiaFundsFetch()\n     const iss = new Set(rows.map(function(f){ return f.issuer; }));\n     document.getElementById(\'sFunds\').textContent = rows.length;\n     document.getElementById(\'sIssuers\').textContent = iss.size;\n-    if (ys.length) document.getElementById(\'sYield\').textContent = ys[Math.floor(ys.length/2)].toFixed(1) + \'%\';\n+    if (ys.length) {\n+      // median: the average of the middle two when the count is even\n+      const m = ys.length >> 1;\n+      const med = ys.length % 2 ? ys[m] : (ys[m-1] + ys[m]) / 2;\n+      document.getElementById(\'sYield\').textContent = med.toFixed(1) + \'%\';\n+    }\n   })\n   .catch(function(){});\n \ndiff --git a/backtest/index.html b/backtest/index.html\nindex 9949fe6..5b70a74 100644\n--- a/backtest/index.html\n+++ b/backtest/index.html\n@@ -826,13 +826,19 @@ async function loadSeries(ticker){\n     .filter(function(p){ return p.length >= 2 && p[1]; });\n   if (rows.length < 10) return null;\n   const steps = (RATIOS[ticker] || {}).r || null;\n+  // The price files carry date,close,adj_close,volume. Reading column 1 as the\n+  // adjusted close drew the price line as the total-return line and divided the\n+  // price by a dividend ratio on top, so the two lines were both wrong.\n+  const wide = rows[0].length >= 3 && rows[0][2];\n   const pts = rows.map(function(p){\n     const day = Math.round(new Date(p[0] + \'T00:00:00Z\').getTime()/86400000);\n-    const adj = Number(p[1]);\n-    const close = steps ? adj / ratioAt(steps, day) : null;\n-    return {d: p[0], day: day, adj: adj, close: close};\n+    const close = Number(p[1]);\n+    const adj = wide ? Number(p[2]) : close;\n+    return {d: p[0], day: day,\n+            adj: adj,\n+            close: wide ? close : (steps ? close / ratioAt(steps, day) : null)};\n   });\n-  return {ticker: ticker, pts: pts, hasClose: !!steps};\n+  return {ticker: ticker, pts: pts, hasClose: wide || !!steps};\n }\n \n function windowed(pts){\ndiff --git a/compare/index.html b/compare/index.html\nindex 560a52c..67cce39 100644\n--- a/compare/index.html\n+++ b/compare/index.html\n@@ -874,6 +874,8 @@\n </div>\n \n <script>\n+function licentiaMedian(a){ if(!a.length) return null; const m = a.length >> 1; return a.length % 2 ? a[m] : (a[m-1] + a[m]) / 2; }\n+\n \n // Locked funds are listed alongside the rest so a manager\'s shelf looks the\n // size it really is, then marked with the tier that opens them. Selecting one\n@@ -1105,7 +1107,7 @@ function buildFamilies(){\n     const colour = famColour(issuer);\n     const cad = list.filter(([,f]) => f.region === \'CAD\').length;\n     const ys = list.map(([,f]) => (f.stats||{}).yield_ttm).filter(v => v !== undefined).sort((a,b)=>a-b);\n-    const med = ys.length ? ys[Math.floor(ys.length/2)] : undefined;\n+    const med = ys.length ? licentiaMedian(ys) : undefined;\n     const sec = document.createElement(\'section\');\n     sec.className = \'fam\';\n     sec.style.setProperty(\'--fam\', colour);\ndiff --git a/fund/index.html b/fund/index.html\nindex 5081cae..e505c5d 100644\n--- a/fund/index.html\n+++ b/fund/index.html\n@@ -450,7 +450,7 @@ const SECTOR_MAP = {\n   "CQP":"Energy","KNTK":"Energy","HESM":"Energy","GEL":"Energy","DKL":"Energy","NEXT":"Energy",\n   "CTRI":"Energy","TNK":"Energy","SU":"Energy","CNQ":"Energy","CVE":"Energy","IMO":"Energy",\n   "TOU":"Energy","ARX":"Energy","WCP":"Energy","OVV":"Energy","ENB":"Energy","TRP":"Energy",\n-  "PPL":"Energy","KEY":"Energy","SHEL":"Energy","BP":"Energy","TTE":"Energy","E":"Energy",\n+  "PPL":"Energy","SHEL":"Energy","BP":"Energy","TTE":"Energy","E":"Energy",\n   "EQNR":"Energy","PBR":"Energy","YPF":"Energy","SSL":"Energy","TS":"Energy","CCJ":"Energy",\n   "CCO":"Energy",\n \n@@ -491,7 +491,7 @@ const SECTOR_MAP = {\n   "TSN":"Consumer Staples","ADM":"Consumer Staples","BG":"Consumer Staples","MDLZ":"Consumer Staples",\n   "CL":"Consumer Staples","KMB":"Consumer Staples","CHD":"Consumer Staples","CLX":"Consumer Staples",\n   "EL":"Consumer Staples","KVUE":"Consumer Staples","UL":"Consumer Staples","FMX":"Consumer Staples",\n-  "MRU":"Consumer Staples","L":"Consumer Staples","WN":"Consumer Staples","SAP":"Consumer Staples",\n+  "MRU":"Consumer Staples","WN":"Consumer Staples","SAP":"Consumer Staples",\n \n   // --- communication services ---\n   "GOOGL":"Communication Services","GOOG":"Communication Services","META":"Communication Services",\n@@ -569,7 +569,7 @@ const SECTOR_MAP = {\n   "DTE":"Utilities","AEE":"Utilities","CMS":"Utilities","CNP":"Utilities","NI":"Utilities",\n   "LNT":"Utilities","EVRG":"Utilities","PNW":"Utilities","ATO":"Utilities","NRG":"Utilities",\n   "VST":"Utilities","CEG":"Utilities","ETR":"Utilities","AES":"Utilities","AWK":"Utilities",\n-  "FTS":"Utilities","EMA":"Utilities","CU":"Utilities","AQN":"Utilities","H":"Utilities",\n+  "FTS":"Utilities","EMA":"Utilities","CU":"Utilities","AQN":"Utilities",\n   "BIP-U":"Utilities","BEPC":"Utilities","NG/":"Utilities","UU/":"Utilities","SVT":"Utilities",\n   "NTGY":"Utilities","VIE":"Utilities","ORA":"Utilities","FORTUM":"Utilities","ENLAY":"Utilities",\n   "RWEOY":"Utilities","EDP":"Utilities",\n@@ -841,6 +841,10 @@ function compositionPanel(f){\n \n     ${distributionSection(f)}\n \n+    ${!rows.length ? `\n+    <div class="section-head"><h2>Portfolio shape</h2></div>\n+    <div class="empty" style="padding:22px;">${f.issuer || \'The issuer\'} doesn\'t publish this fund\'s\n+    holdings anywhere we can read, so there is nothing to show here rather than a guess.</div>` : `\n     <div class="section-head"><h2>Portfolio shape</h2></div>\n     <div class="stat-strip">\n       <div class="stat">\n@@ -868,11 +872,12 @@ function compositionPanel(f){\n         <div class="stat-value" style="font-size:20px;">${structure}</div>\n         <div class="stat-sub">${isWrapper ? fmt(fundWeight/total*100,0) + \'% held via other funds\' : \'holds securities directly\'}</div>\n       </div>\n-    </div>\n+    </div>`}\n \n     ${isWrapper ? lookthroughSection(fundHoldings, total) : \'\'}\n-    ${sectorSection(rows, total, isWrapper)}\n+    ${rows.length ? sectorSection(rows, total, isWrapper) : \'\'}\n \n+    ${!rows.length ? \'\' : `\n     <div class="section">\n       <div class="section-head">\n         <h2>All holdings</h2>\n@@ -884,10 +889,12 @@ function compositionPanel(f){\n         <button class="sort-btn" data-sort="ticker">A–Z</button>\n       </div>\n       <div id="tableWrap"></div>\n-    </div>\n+    </div>`}\n \n     <div class="data-note">\n-      Holdings are pulled straight from ${f.issuer || \'the issuer\'}\'s published holdings file and refreshed daily.\n+      ${rows.length\n+        ? `Holdings are pulled straight from ${f.issuer || \'the issuer\'}\'s published holdings file and refreshed daily.`\n+        : `${f.issuer || \'The issuer\'} publishes no holdings file we can read for this fund, so only its price, size and distributions are shown.`}\n       Sector labels are our own mapping, not the issuer\'s — treat them as a rough shape, not a classification.\n       Yield and expense labels are reproduced exactly as the issuer words them — a 30-day SEC yield and a\n       trailing 12-month distribution yield are not the same number and are not comparable across issuers.\n@@ -899,26 +906,32 @@ function compositionPanel(f){\n   if(more){\n     more.addEventListener(\'click\', () => {\n       const body = document.getElementById(\'distRows\');\n-      body.innerHTML = (f.distributions || []).map(x =>\n+      body.innerHTML = d.map(x =>\n         \'<tr><td>\' + x.ex_date + \'</td><td>\' + (x.pay_date || \'—\') +\n         \'</td><td class="amt">$\' + x.amount.toFixed(4) + \'</td></tr>\').join(\'\');\n       more.remove();\n     });\n   }\n \n-  document.getElementById(\'search\').addEventListener(\'input\', e => {\n-    FILTER = e.target.value.trim().toUpperCase();\n-    renderTable(rows, total);\n-  });\n-  document.querySelectorAll(\'.sort-btn\').forEach(b => {\n-    b.addEventListener(\'click\', () => {\n-      document.querySelectorAll(\'.sort-btn\').forEach(x => x.classList.remove(\'active\'));\n-      b.classList.add(\'active\');\n-      SORT = b.dataset.sort;\n+  // A fund with no published holdings has no holdings table, so there is\n+  // nothing to filter or sort — and binding to it threw before the rest of\n+  // the page could finish.\n+  const search = document.getElementById(\'search\');\n+  if (search) {\n+    search.addEventListener(\'input\', e => {\n+      FILTER = e.target.value.trim().toUpperCase();\n       renderTable(rows, total);\n     });\n-  });\n-  renderTable(rows, total);\n+    document.querySelectorAll(\'.sort-btn\').forEach(b => {\n+      b.addEventListener(\'click\', () => {\n+        document.querySelectorAll(\'.sort-btn\').forEach(x => x.classList.remove(\'active\'));\n+        b.classList.add(\'active\');\n+        SORT = b.dataset.sort;\n+        renderTable(rows, total);\n+      });\n+    });\n+    renderTable(rows, total);\n+  }\n }\n \n function asOfLine(f){\n@@ -976,12 +989,19 @@ function yieldContext(st){\n // The next payment, announced by the issuer or projected from the fund\'s own\n // rhythm. A projection is always labelled as one: an income investor deciding\n // whether to buy before an ex-date must not mistake a guess for a declaration.\n+// The reader\'s own date. Using the UTC date made a payment that goes ex\n+// tomorrow read as already paid every evening after 8pm Eastern.\n+function licentiaToday(){\n+  const d = new Date();\n+  return new Date(d.getTime() - d.getTimezoneOffset() * 60000).toISOString().slice(0, 10);\n+}\n+\n function nextPaymentCard(st){\n   if(!st.next_ex_date || st.next_amount === undefined) return \'\';\n   const fmtDay = s => new Date(s + \'T12:00:00Z\').toLocaleDateString(\'en-CA\',\n     {month:\'short\', day:\'numeric\', timeZone:\'UTC\'});\n-  const today = new Date(); today.setUTCHours(0,0,0,0);\n-  const days = Math.round((new Date(st.next_ex_date + \'T00:00:00Z\') - today) / 86400000);\n+  const days = Math.round((new Date(st.next_ex_date + \'T12:00:00Z\')\n+                           - new Date(licentiaToday() + \'T12:00:00Z\')) / 86400000);\n   if(days < 0) return \'\';               // stale: the date has already passed\n   const when = days === 0 ? \'today\' : days === 1 ? \'tomorrow\' : \'in \' + days + \' days\';\n   const announced = st.next_status === \'announced\';\n@@ -1001,7 +1021,7 @@ function nextPaymentCard(st){\n function distributionSection(f){\n   // an issuer table can list the announced next payment; that is shown in its\n   // own card, so history and "latest payment" only cover payments already ex\n-  const _today = new Date().toISOString().slice(0, 10);\n+  const _today = licentiaToday();\n   const d = (f.distributions || []).filter(x => x.ex_date <= _today);\n   const st = f.stats || {};\n   if(!d.length){\n@@ -1073,10 +1093,12 @@ function distributionSection(f){\n         <div class="stat-value">$${Number(st.ttm_total).toFixed(2)}</div>\n         <div class="stat-sub">${st.ttm_payments || \'\'} payments</div>\n       </div>` : \'\'}\n-      ${st.total_return_1y !== undefined ? `<div class="stat">\n+      ${st.total_return_1y !== undefined && st.total_return_1y !== null ? `<div class="stat">\n         <div class="stat-label">Total return</div>\n         <div class="stat-value">${st.total_return_1y > 0 ? "+" : ""}${st.total_return_1y}%</div>\n-        <div class="stat-sub">last 12 months, distributions included</div>\n+        <div class="stat-sub">${st.return_window === \'since inception\'\n+          ? \'since inception, distributions included\'\n+          : \'last 12 months, distributions included\'}</div>\n       </div>` : ""}\n       ${st.frequency ? `<div class="stat">\n         <div class="stat-label">Frequency</div>\ndiff --git a/funds/index.html b/funds/index.html\nindex 317693b..2173a4e 100644\n--- a/funds/index.html\n+++ b/funds/index.html\n@@ -516,6 +516,8 @@\n \n </main>\n <script>\n+function licentiaMedian(a){ if(!a.length) return null; const m = a.length >> 1; return a.length % 2 ? a[m] : (a[m-1] + a[m]) / 2; }\n+\n const FALLBACK_FUNDS = {};\n let FUNDS = {};\n \n@@ -867,7 +869,7 @@ function marketStats(pred, regionKey){\n   return {\n     funds: list.length,\n     issuers: Object.keys(counts).length,\n-    medYield: ys.length ? ys[Math.floor(ys.length/2)] : null,\n+    medYield: licentiaMedian(ys),\n     aum: aum,\n     topIssuers: Object.entries(counts).sort(function(a,b){ return b[1]-a[1]; })\n                  .slice(0,5).map(function(p){ return p[0]; })\n@@ -957,7 +959,7 @@ function buildAside(issuer, funds){\n   paintFam(el, issuer);\n   const ys = funds.map(function(f){ return (f[1].stats||{}).yield_ttm; })\n                   .filter(function(v){ return v !== undefined; }).sort(function(a,b){ return a-b; });\n-  const med = ys.length ? ys[Math.floor(ys.length/2)] : undefined;\n+  const med = ys.length ? licentiaMedian(ys) : undefined;\n   const aum = funds.reduce(function(t,f){ return t + (((f[1].stats||{}).aum_musd)||0); }, 0);\n   const cad = funds.filter(function(f){ return f[1].region === \'CAD\'; }).length;\n   el.innerHTML =\ndiff --git a/portfolio/index.html b/portfolio/index.html\nindex c7ad20d..65404a4 100644\n--- a/portfolio/index.html\n+++ b/portfolio/index.html\n@@ -813,7 +813,7 @@ function drawShareCard(showMoney){\n   function money(n){ return "$" + Math.round(n).toLocaleString("en-CA"); }\n \n   if (showMoney && d.value){\n-    fig("Invested", money(d.value), 64, 196);\n+    fig("Value today", money(d.value), 64, 196);\n     fig("Income a year", money(d.income), 424, 196);\n     fig("Income a month", money(d.income / 12), 784, 196);\n     x.fillStyle = "#8A9099";\n@@ -956,8 +956,8 @@ function openShareCard(){\n       + \'<div class="sm-preview" id="smPreview"></div>\'\n       + \'<label class="sm-toggle"><input type="checkbox" id="smMoney" checked>\'\n         + \'<span>Include the dollar amounts</span></label>\'\n-      + \'<p class="sm-note">Off by default. The card still shows your yield, \'\n-        + \'Your yield, mix and holdings still show.</p>\'\n+      + \'<p class="sm-note">On by default. Untick it and the card still shows \'\n+        + \'your yield, your mix and your holdings, but no dollar amounts.</p>\'\n       + \'<div class="sm-acts">\'\n         + \'<button class="sm-go" id="smSave">Download the image</button>\'\n         + \'<button class="sm-alt" id="smShare">Share&hellip;</button>\'\ndiff --git a/scraper.py b/scraper.py\nindex e4544eb..34a7184 100644\n--- a/scraper.py\n+++ b/scraper.py\n@@ -2565,13 +2565,18 @@ def attach_price_and_yield(registry: list) -> None:\n         # site needs both numbers side by side, not the yield alone.\n         try:\n             rows = [ln.split(",") for ln in csv_path.read_text().strip().splitlines()[1:]]\n-            series = [(r[0], float(r[1])) for r in rows if len(r) >= 2]\n+            # Column 2 is the adjusted close, which already reinvests every\n+            # distribution; column 1 is the market price. Measuring return from\n+            # column 1 and labelling it "distributions included" understated\n+            # every fund by its own yield (HMAX: +9% instead of +80%).\n+            series = [(r[0], float(r[2])) for r in rows if len(r) >= 3]\n             if len(series) > 30:\n                 cutoff = (datetime.now(timezone.utc).date() - timedelta(days=365)).isoformat()\n                 past = [p for d0, p in series if d0 <= cutoff]\n                 start = past[-1] if past else series[0][1]\n+                end = series[-1][1]\n                 if start > 0:\n-                    fund.stats["total_return_1y"] = round((close / start - 1) * 100, 2)\n+                    fund.stats["total_return_1y"] = round((end / start - 1) * 100, 2)\n                     fund.stats["return_window"] = "1y" if past else "since inception"\n         except Exception:  # noqa: BLE001\n             pass\n'

report = {}
Path("data").mkdir(exist_ok=True)
Path("/tmp/fix.patch").write_text(PATCH)
r = subprocess.run(["git", "apply", "--check", "/tmp/fix.patch"], capture_output=True, text=True)
if r.returncode == 0:
    subprocess.run(["git", "apply", "/tmp/fix.patch"], check=True)
    report["applied"] = True
else:
    report["applied"] = False
    report["error"] = r.stderr[:300]

t = Path("scraper.py").read_text()
try:
    ast.parse(t); report["parses"] = True
except SyntaxError as exc:
    report["parses"] = False; report["syntax"] = str(exc)[:200]
report["totalReturnFromAdjusted"] = "float(r[2])" in t
fund = Path("fund/index.html").read_text()
report["fundPage"] = "licentiaToday" in fund and fund.count('"KEY":') == 1
report["median"] = "licentiaMedian" in Path("funds/index.html").read_text()

Path("data/api-probe.json").write_text(json.dumps(report, indent=2))
print(json.dumps(report, indent=2))
if report["applied"] and report["parses"]:
    subprocess.run(["git", "config", "user.name", "ledger-bot"], check=False)
    subprocess.run(["git", "config", "user.email", "bot@users.noreply.github.com"], check=False)
    subprocess.run(["git", "add", "-A"], check=False)
    subprocess.run(["git", "commit", "-m", "Fix eight bugs found auditing the pages: total return, sector map, medians, empty holdings"], check=False)
    subprocess.run(["git", "push"], check=False)
